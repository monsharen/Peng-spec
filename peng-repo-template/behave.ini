[behave]
# Feature files live in peng-spec (sibling checkout or submodule)
# Override with: behave <path-to-features>
paths = ../peng-spec/features
steps_dir = tests/steps
junit = true
junit_directory = test-results
format = pretty
